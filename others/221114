221114 작업 내용

***** 다이어리 앱 만들기 실습 
// https://thebook.io/080236/ch06/01/

- useContext

- useRef
// 함수 컴포넌트에서 컴포넌트의 레퍼런스를 선택할 수 있게 하는 Hook

- KeyboardAvoidingView(ios)

- uuid 라이브러리
// npm i uuid
// 이 라이브러리는 Node.js의 crypto 기능을 사용하는데, 리액트 네이티브에는 이 기능이 기본적으로 내장되어 있지 않다. 
// 따라서 이 라이브러리가 정상적으로 작동할 수 있도록
- react-native-get-random-values 라이브러리
// 프로젝트 최상위 디렉터리에 있는 index.js 파일에 
// import 'react-native-get-random-values'; 추가

// setLogs([log, ...logs]);
// log가 ...log보다 먼저 오면 새로 만든 객체가 맨 앞에 추가

// console.log(JSON.stringify(logs, null, 2));
// JSON.stringify()를 사용할 때 두 번째와 세 번째 파라미터를 이와 같이 
// 설정해주면 객체나 배열을 출력할 때 공백과 새 줄을 포함해 보기 좋게 출력

- FeedListItem 컴포넌트
// log 객체를 Props로 받아와서 알맞은 위치에 정보 보여주기
